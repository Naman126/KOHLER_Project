# KOHLER_Project
An AI-powered design assistant that converts natural-language constraints into viable KOHLER product bundles. It uses a deterministic, zero-hallucination math engine to strictly enforce physical room dimensions and budget limits, while rendering the final design in a procedural, style-specific 3D environment using Three.js and Streamlit.
